# Mirokaï DOA (minimal)
Speech-only DOA: features, SRP baseline, tiny model stubs, training hooks, realtime skeleton.

## Quick start
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
pre-commit install
pytest -q
